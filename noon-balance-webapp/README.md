# 午時評術 Noon Balance

一個簡單的 Web App 首頁，用來做正午狀態自評，並產生平衡分數與建議。

## 使用方式

1. 直接用瀏覽器打開 `index.html`。
2. 或在資料夾內啟動本機伺服器：

```bash
cd /Users/kenchwong/Desktop/noon-balance-webapp
python3 -m http.server 8080
```

3. 開啟 `http://localhost:8080`。
